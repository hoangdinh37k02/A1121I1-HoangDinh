let inputWidth;
let inputHeight;
inputWidth = prompt("Input the width");
inputHeight = prompt("Input the height");

width = parseInt(inputWidth);
height = parseInt(inputHeight);

area = width * height;

document.write("This area is: " + area)